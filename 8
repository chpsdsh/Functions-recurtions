#include <stdio.h>

int IsPrime(int n, int s){
    if (s <=1)
        return 1;
    else
        if (n % s !=0)
            return IsPrime(n,s - 1);
        else
            return 0;
}


int main() {
    int n;
    scanf_s("%d", &n);
    printf("%d", IsPrime(n,n-1));
    return 0;

}
