#include <stdio.h>

int SummaCifr(int n){
    if (n < 10)
        return n;
    else
        return  n%10 + SummaCifr(n / 10);
}

int main() {
    int n;
    scanf_s("%d", &n);
    printf("%d", SummaCifr(n));
    return 0;

}
